Algoritmo "exercicio20"

Var
    x, y, i, z: real

Inicio

    escreva ("Informe o número de dias trabalhados: ")
    leia (x)
    y <- x * 20
    i <- 0.08 * y
    z <- y - i
    escreval
    escreva ("Quantidade liquida a ser paga: ", z)

Fimalgoritmo